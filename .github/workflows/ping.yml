name: Ping Render

on:
  schedule:
    - cron: '*/5 * * * *'  # co 5 minut (w UTC)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://next-planning-poker.onrender.com/health
